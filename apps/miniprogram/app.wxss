page {
  min-height: 100vh;
  color: #251c38;
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  background: radial-gradient(circle at 12% 8%, rgba(201, 166, 107, 0.16), transparent 28%), linear-gradient(180deg, #faf7f2 0%, #f5f1ec 100%);
}

view, text, button {
  box-sizing: border-box;
}

button {
  margin: 0;
  padding: 0;
  border: 0;
  line-height: 1;
  background: transparent;
  font-family: inherit;
}

button::after {
  border: 0;
}

.page {
  min-height: 100vh;
  padding: 88rpx 32rpx 40rpx;
  background: radial-gradient(circle at 86% 2%, rgba(90, 70, 128, 0.18), transparent 32%), linear-gradient(180deg, #fdfbf7 0%, #f5f1ec 72%, #ebe5df 100%);
}

.nav-title {
  font-size: 42rpx;
  line-height: 1.15;
  font-weight: 800;
  letter-spacing: -1rpx;
  color: #251c38;
}

.nav-subtitle {
  margin-top: 10rpx;
  color: #8b8199;
  font-size: 24rpx;
}

.card {
  border-radius: 42rpx;
  background: rgba(253, 251, 247, 0.86);
  border: 1rpx solid rgba(255, 255, 255, 0.78);
  box-shadow: 0 28rpx 70rpx rgba(70, 52, 29, 0.1);
}

.gold-button {
  height: 88rpx;
  padding: 0 38rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #251c38;
  font-size: 28rpx;
  font-weight: 800;
  background: linear-gradient(145deg, #fff1c8, #c9a66b);
  box-shadow: 0 18rpx 38rpx rgba(155, 116, 58, 0.24);
}

.dark-button {
  height: 88rpx;
  padding: 0 38rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fdfbf7;
  font-size: 28rpx;
  font-weight: 800;
  background: linear-gradient(145deg, #3b2b57, #251c38);
  box-shadow: 0 18rpx 38rpx rgba(37, 28, 56, 0.2);
}

.muted {
  color: #7c7288;
}

.amount {
  font-family: DIN Alternate, SF Pro Display, Arial, sans-serif;
  letter-spacing: -1rpx;
}

/* ---- 自定义导航（三个 tab 页共用，标题行与系统胶囊同行对齐） ---- */
.page.has-navbar {
  padding-top: 0;
}

.nav-row {
  display: flex;
  align-items: center;
}

.nav-row-title {
  font-size: 36rpx;
  font-weight: 800;
  letter-spacing: -0.5rpx;
  color: #251c38;
  /* 右侧空间留给系统胶囊 */
  max-width: 480rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.nav-sub-row {
  margin-top: 10rpx;
  color: #8b8199;
  font-size: 24rpx;
}
